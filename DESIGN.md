# Fig.md 디자인 시스템

> 이 문서가 Fig.md의 시각·인터랙션 기준선(source of truth)이다. 패널 UI와 렌더된 문서
> 프레임 양쪽에 적용한다. 값은 토큰으로 관리하고, 하드코딩 대신 이 이름을 참조한다.
> **레퍼런스: Apple iOS 디자인(HIG)** — system 색, SF Pro, 그룹형 인셋 레이아웃.

## 기억시킬 한 가지 (memorable thing)
**"차분하고 정밀한 도구."** 매일 열어도 부담 없고, 마크다운을 피그마 문서로 바꾸는 일이
신뢰감 있게 느껴져야 한다. 화려함이 아니라 **절제·정렬·예측 가능성**이 인상이다.

## 원칙
1. **메인은 두 가지만** — 마크다운 입력창 + 두 방향 액션(만들기/추출). 나머지는 뒤로.
2. **점진 공개** — 자주 안 바꾸는 것(색상 오버라이드)은 접어둔다.
3. **컨텍스트 노출** — 삽입(구분선·표)은 상시 버튼이 아니라 편집 중일 때만 "+"로 뜬다(Notion 감각).
4. **한 곳에만 색** — 액센트(systemBlue)는 주 동작 1개·포커스에만. 나머지는 중립 회색.
5. **양방향을 눈에 보이게** — 화살표(↓ 만들기 / ↑ 추출)로 방향 명시.
6. **군더더기 제거** — 헤더 태그 등 정보 없는 장식은 없앤다(워드마크만).

## 타이포그래피
- **기본 폰트: SF Pro** — 스택 `"SF Pro Text","SF Pro Display","SF Pro",-apple-system,
  BlinkMacSystemFont,system-ui,"Segoe UI","Malgun Gothic",sans-serif`.
  - Mac/iOS: 내장 SF Pro로 렌더. **Windows: 미설치라 Segoe UI로 폴백** — 진짜 SF Pro가
    필요하면 Apple에서 SF Pro 설치 또는 @font-face 임베드(라이선스·용량 고려).
- **모노**: `ui-monospace,"SF Mono","SFMono-Regular",Menlo,Consolas` — 입력창·코드.
- **스케일(패널)**: 라벨 11(600, 대문자) / 캡션 11.5 / 본문·행 14 / 버튼 14(600) / 워드마크 21(700).
- **렌더 문서 제목**: H1 32 / H2 24 / H3 19 (Bold), 본문 16, 코드 13.

## 색상 토큰 (iOS system 색 기반) — 패널 UI
| 토큰 | 라이트 | 다크 | 대응 iOS |
|---|---|---|---|
| `--bg` | `#F2F2F7` | `#000000` | systemGroupedBackground |
| `--card` | `#FFFFFF` | `#1C1C1E` | (secondary) systemGroupedBackground |
| `--sep` | `rgba(60,60,67,.29)` | `rgba(84,84,88,.65)` | separator |
| `--label` | `#000000` | `#FFFFFF` | label |
| `--secondary` | `rgba(60,60,67,.6)` | `rgba(235,235,245,.6)` | secondaryLabel |
| `--tertiary` | `rgba(60,60,67,.32)` | `rgba(235,235,245,.3)` | tertiaryLabel |
| `--tint` | `#007AFF` | `#0A84FF` | systemBlue (유일 액센트) |
| `--tint-weak` | `rgba(0,122,255,.12)` | `rgba(10,132,255,.20)` | tint 배경 틴트 |

패널은 뷰어(피그마) 라이트/다크에 자동 대응: `prefers-color-scheme` + `data-theme` 오버라이드.

## 렌더된 문서 프레임 — 플러그인 테마를 따름
- **문서 프레임의 라이트/다크는 플러그인(=피그마) 테마를 그대로 상속한다.** 별도 토글 없음.
- 사용자가 원하면 **색상 직접 지정**(배경/글자)으로 오버라이드 가능(기본 접힘).
- 테마별 기본 색:

| | 라이트 | 다크 |
|---|---|---|
| 배경 | `#FFFFFF` | `#1C1C1E` |
| 글자 | `#000000` | `#FFFFFF` |
| 구분선/표 라인 | `#D1D1D6` | `#38383A` |
| 인용 콜아웃 배경 | `#F2F2F7` | `#2C2C2E` |
| 코드블록 배경 | `#F2F2F7` | `#2C2C2E` |

→ 구현 시 render.ts의 색을 이 토큰으로 뽑아내고, UI가 (테마 + 선택적 오버라이드 색)을
render 메시지로 전달. 다크에서 구분선·표·콜아웃·코드 배경이 자연스럽게 반전되도록 처리.

## 간격·형태·모션 (iOS 감각)
- **간격**: 8px 그리드(6·8·12·15·16). iOS 행 높이 최소 44.
- **라운드**: 카드/그룹 12 · 버튼 13 · 컨트롤 8–9 · 팝오버 14.
- **테두리/구분선**: 1px `--sep`. 그룹 내 행 사이 인셋 세퍼레이터.
- **모션**: hover/press 100–150ms. 팝오버 fade+scale 140ms(cubic-bezier(.2,.8,.3,1)).
  색상 펼치기 max-height 220ms. `prefers-reduced-motion` 존중.
- **포커스**: `outline:2px var(--tint)` 항상 표시. 입력 포커스는 tint 보더 + 3px tint-weak 링.

## 컴포넌트·인터랙션 스펙
- **헤더**: 워드마크 `Fig.md`만(`.md`는 tint). 우상단 태그 없음(제거).
- **두 방향 버튼(iOS)**: 높이 50, 라운드 13. 만들기=systemBlue 채움(primary),
  추출=tinted(systemBlue 텍스트 + tint-weak 배경). 각 버튼 화살표(↓/↑) + 캡션.
- **컨텍스트 "+"**: 입력창 라벨 우측. 편집(focus) 전 비활성(opacity .35), 편집 시 활성.
  클릭 → 팝오버: 구분선(즉시) / 표(옆 `행x열` 입력 + 삽입). 커서 위치 삽입. Esc·바깥 클릭 닫힘.
- **표 규격**: `행x열`(예 `3x2`), 첫 행 항상 헤더. `×` 허용. 상한 20행×10열.
- **문서 스타일 그룹(iOS 그룹형)**: "색상 직접 지정" 행(펼치기) → 배경/글자 피커 + 미리보기.
  기본 접힘. 그 아래 각주: "문서는 플러그인 테마를 따릅니다."

## 안티패턴 (피할 것)
- Win98식 회색 입체 버튼·기본 폼 컨트롤.
- 정보 없는 헤더 장식/태그.
- 상시 노출 삽입 칩 무더기(메인 어지럽힘).
- 액센트 남발(systemBlue 외 다른 색이 힘줌).
- 보라→파랑 그라데이션, 이모지 섹션 마커, 전부 가운데정렬 같은 AI 기본 룩.

## 참고
- iOS 시스템 색·레이아웃: Apple Human Interface Guidelines.
- 컨텍스트 삽입 메뉴: Notion `/`·`+` 블록 삽입.
- 점진 공개: Nielsen progressive disclosure(초기 완료 30–50% 빠름).
- 목업(인터랙티브): scratchpad `fig-md-panel-mockup.html` → Artifact.
