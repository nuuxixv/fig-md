# Fig.md 디자인 시스템

> Fig.md의 시각·인터랙션 기준선(source of truth). 패널 UI + 렌더 문서 프레임 양쪽에 적용.
> 값은 토큰으로 관리하고 하드코딩 대신 이 이름을 참조한다.
> **레퍼런스: iOS 26 "Liquid Glass"** — 반투명 유리 재질, blur, 스펙큘러 하이라이트, 큰 concentric 라운드.

## 기억시킬 한 가지 (memorable thing)
**"차분하고 정밀한 도구."** 매일 열어도 부담 없고, 마크다운을 피그마 문서로 바꾸는 일이
신뢰감 있게 느껴져야 한다. 절제·정렬·예측 가능성이 인상.

## 원칙
1. 메인은 둘만 — 입력창 + 두 방향 액션(만들기/추출). 나머지는 뒤로.
2. 점진 공개 — 자주 안 바꾸는 것(색 오버라이드)은 접어둔다.
3. 컨텍스트 노출 — 삽입(구분선·표)은 편집 중일 때만 "+"로.
4. 한 곳에만 색 — 액센트(systemBlue)는 주 동작·포커스에만.
5. 방향을 눈에 — 아이콘·캡션으로 만들기/추출 방향 명시.
6. 군더더기 제거 — 헤더 태그·장식 그라데이션 없음.

## 테마 모델 — 현재 Figma "페이지 배경"에 맞춤 (2026-07-23 확정)
시스템/Figma UI 테마가 아니라 **현재 작업 중인 페이지의 배경색**을 기준으로 한다.
(이유: 페이지는 아무 배경색이나 가질 수 있고, UI 라이트/다크는 그걸 반영 못 함.)

- **판정**: `figma.currentPage.backgrounds`(SOLID) 색의 **밝기(luminance)로 라이트/다크 결정**.
  → 패널과 문서 프레임 **둘 다** 이 판정을 따른다.
- **채도 높은 페이지색 방어**: 그 색을 그대로 칠하지 않는다. **밝기만 참조**해 라이트/다크를
  고르고, 패널은 중립 유리 팔레트 유지(+ 아주 은은한 틴트까지만). 가독성 우선.
- **수동 오버라이드**: 문서는 사용자가 배경/글자색 직접 지정 가능(기본 접힘).
- **갱신**: 페이지 배경 변경 반영은 이벤트 구독(`figma.on(...)`)으로. (초기엔 렌더/열기 시 1회 읽기로 시작해도 됨.)
- **한계(문서화)**: Figma 플러그인은 iframe이라 **뒤의 Figma 캔버스를 blur로 비출 수 없다.**
  Liquid Glass의 "비침"은 목업 전용. 실제 패널은 자기 단색 배경(페이지색 기반) 위에서
  반투명·하이라이트·라운드로만 유리감을 낸다.

## 타이포그래피
- **패널 UI**: **Pretendard**(임베드) — SF Pro/Inter 계열 + 완벽한 한글, Windows 폴백 문제 없음.
  스택 `"Pretendard","SF Pro Text",-apple-system,system-ui,"Segoe UI","Malgun Gothic",sans-serif`.
- **렌더 문서**: 기본 **Inter**(Figma 항상 가용, 안전). 한글은 Figma 폴백.
  사용자 Figma에 Pretendard가 있으면 문서도 Pretendard로 전환 가능(선택).
- **모노**: `ui-monospace,"SF Mono","SFMono-Regular",Menlo,Consolas` — 입력창·코드.
- **스케일(패널)**: 라벨 11(600, 대문자) / 캡션 11.5 / 본문·행 14 / 버튼 14(600) / 워드마크 22(700).
- **렌더 문서 제목**: H1 32 / H2 24 / H3 19 (Bold), 본문 16, 코드 13.

## 색상 토큰 (Liquid Glass · systemBlue 액센트)
반투명 유리 재질. 라이트/다크는 위 "페이지 배경" 판정으로 스위칭.

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--label` | `#0C1220` | `#F3F5FB` | 본문 |
| `--secondary` | `rgba(28,32,46,.62)` | `rgba(232,235,245,.64)` | 보조 |
| `--tertiary` | `rgba(28,32,46,.38)` | `rgba(232,235,245,.36)` | 힌트 |
| `--tint` / solid | `#007AFF` | `#0A84FF` | systemBlue(유일 액센트) |
| `--glass`(패널) | `rgba(255,255,255,.52)` | `rgba(38,41,52,.50)` | 유리 표면(+backdrop-blur 30) |
| `--glass-2`(컨트롤) | `rgba(255,255,255,.46)` | `rgba(255,255,255,.07)` | 인셋·틴트 유리 |
| `--gborder` | `rgba(255,255,255,.65)` | `rgba(255,255,255,.16)` | 유리 테두리 |
| `--ghi`(하이라이트) | `rgba(255,255,255,.85)` | `rgba(255,255,255,.22)` | 상단 스펙큘러(inset 0 1px) |
| 무대(패널 뒤) | 페이지 배경 단색(예 `#ECECEF`) | 페이지 배경 단색(예 `#242428`) | 그라데이션 없음 |

- 유리: `backdrop-filter:blur(30px) saturate(185%)` + `inset 0 1px 0 var(--ghi)` + 부드러운 큰 shadow.
- **그라데이션·색 blob 배경 없음**(제거됨).

## 렌더된 문서 프레임 — 테마별 기본색 (페이지 배경 판정 결과)
| | 라이트 | 다크 |
|---|---|---|
| 배경 | `#FFFFFF` | `#1C1C1E` |
| 글자 | `#000000` | `#FFFFFF` |
| 구분선/표 라인 | `#D1D1D6` | `#38383A` |
| 인용 콜아웃 배경 | `#F2F2F7` | `#2C2C2E` |
| 코드블록 배경 | `#F2F2F7` | `#2C2C2E` |
→ render.ts 색을 토큰화하고, UI가 (페이지배경 판정 라이트/다크 + 선택 오버라이드 색)을 render 메시지로 전달.

## 간격·형태·모션 (iOS 26)
- 간격: 8px 그리드(6·8·12·15·16). 행 최소 46.
- 라운드(concentric): 패널 28 · 그룹/입력 18 · 버튼 18 · 컨트롤 11 · 팝오버 20 · 필 999.
- 모션: hover/press 100–160ms. 팝오버 fade+scale 160ms cubic-bezier(.2,.85,.3,1). 색상 펼치기 max-height 220ms. `prefers-reduced-motion` 존중.
- 포커스: `outline:2px var(--tint)`; 입력 포커스는 tint 보더 + 4px tint 링.

## 컴포넌트·인터랙션 스펙
- **헤더**: 워드마크 `Fig.md`만(`.md`는 tint). 우상단 태그 없음.
- **두 방향 버튼**: 높이 54, 라운드 18, 유리+하이라이트. 만들기=systemBlue 광택 그라데이션(버튼 자체 광택은 유지, 배경 그라데이션과 다름), 추출=틴트 유리. 각 버튼 SF 스타일 아이콘.
- **아이콘(SF Symbols 스타일, 직접 그림 — 원본 미사용)**: `+`=플러스 · 만들기=프레임+플러스 ·
  추출=Apple 공유 은유(상자+위로 나가는 화살표) · 표=그리드 · 구분선=수평선. 라이선스상 SF Symbols
  원본은 쓰지 않고 같은 은유로 직접 그린다(또는 오픈셋 Lucide).
- **컨텍스트 "+"**: 입력창 라벨 우측. 편집(focus) 전 비활성, 편집 시 활성. 클릭 → 유리 팝오버:
  구분선(즉시) / 표(옆 `행x열` 입력 + 삽입). 커서 위치 삽입. Esc·바깥 클릭 닫힘.
- **표 규격**: `행x열`(예 `3x2`), 첫 행 헤더. `×` 허용. 상한 20행×10열.
- **문서 스타일 그룹**: "색상 직접 지정" 펼치기 → 배경/글자 피커 + 미리보기(기본 접힘).
  각주: "문서는 현재 페이지 배경을 따릅니다."

## 안티패턴 (피할 것)
- Win98식 회색 입체 버튼·기본 폼 컨트롤.
- 장식용 컬러 그라데이션·무지개 blob(제거함).
- 정보 없는 헤더 태그.
- 상시 노출 삽입 칩 무더기.
- systemBlue 외 액센트 남발.
- SF Symbols 원본 무단 임베드(라이선스).

## 참고
- iOS 26 Liquid Glass · system 색: Apple HIG.
- 컨텍스트 삽입: Notion `/`·`+`. 점진 공개: Nielsen.
- 목업(인터랙티브): scratchpad `fig-md-panel-mockup.html` → Artifact.
