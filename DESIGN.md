# Fig.md 디자인 시스템

> 이 문서가 Fig.md의 시각·인터랙션 기준선(source of truth)이다. 패널 UI와 렌더된 문서
> 프레임 양쪽에 적용한다. 값은 토큰으로 관리하고, 하드코딩 대신 이 이름을 참조한다.

## 기억시킬 한 가지 (memorable thing)
**"차분하고 정밀한 도구."** 매일 열어도 부담 없고, 마크다운을 피그마 문서로 바꾸는 일이
신뢰감 있게 느껴져야 한다. 화려함이 아니라 **절제·정렬·예측 가능성**이 이 도구의 인상이다.
→ 모든 결정은 이 문장에 복무한다. 크롬(장식)은 최소, 위계는 선명, 색은 한 곳에만 힘.

## 원칙
1. **메인은 두 가지만** — 마크다운 입력창 + 두 방향 액션(만들기/추출). 나머지는 뒤로.
2. **점진 공개** — 자주 안 바꾸는 것(테마 색상)은 접어둔다. 필요할 때만 펼친다.
3. **컨텍스트 노출** — 삽입(구분선·표)은 상시 버튼이 아니라 편집 중일 때만 "+"로 뜬다(Notion 감각).
4. **한 곳에만 색** — 액센트(인디고)는 주 동작 1개에만. 나머지는 중립 회색으로 조용히.
5. **양방향을 눈에 보이게** — 화살표(↓ 만들기 / ↑ 추출)로 방향을 항상 명시.

## 색상 토큰 (패널 UI)
쿨그레이 중립(약한 청색 편향) + 인디고 액센트 1색.

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--bg` | `#E7E8EE` | `#0E0F12` | 캔버스(패널 뒤) |
| `--panel` | `#FFFFFF` | `#1B1C21` | 패널 표면 |
| `--surface` | `#F5F6F9` | `#25262C` | 입력·인셋 필드 |
| `--border` | `#E4E6EC` | `#2E3038` | 경계선 |
| `--text` | `#191A20` | `#ECEDF1` | 본문 |
| `--muted` | `#6C7280` | `#9096A0` | 보조 텍스트·라벨 |
| `--accent` | `#5B6CFF` | `#8592FF` | 주 동작·포커스링(유일 액센트) |
| `--accent-weak` | `rgba(91,108,255,.10)` | `rgba(133,146,255,.16)` | 액센트 배경 틴트 |

패널은 뷰어(피그마) 라이트/다크에 자동 대응: `prefers-color-scheme` + `data-theme` 오버라이드.

## 렌더된 문서 프레임 (별도 테마)
문서 프레임은 **자체 라이트/다크 프리셋 + 커스텀 색**을 가진다(패널 테마와 독립).

| | 라이트 | 다크 |
|---|---|---|
| 배경 | `#FFFFFF` | `#1E1E1E` |
| 글자 | `#191A20` | `#EDEDED` |
| 구분선/표 라인 | `#CCCCCC` 계열 | `#3A3A3A` 계열 |
| 인용 콜아웃 배경 | `#F5F5F5` | `#2A2A2A` |
| 코드블록 배경 | `#F2F2F2` | `#2A2A2A` |

→ 구현 시 render.ts의 색을 이 토큰으로 뽑아내고, UI에서 테마·색을 render 메시지로 전달.

## 타이포그래피
- **UI**: system-ui 스택 (`-apple-system, "Segoe UI", "Malgun Gothic"` 등). 플러그인 패널 관례.
- **모노**: `ui-monospace, "SFMono-Regular", Menlo, Consolas` — 입력창·코드.
- **스케일(패널)**: 라벨 10.5 / 캡션 11 / 본문·버튼 12.5–13 / 워드마크 15. 굵기 500/600/700.
- **렌더 문서 제목**: H1 32 / H2 24 / H3 19 (Bold), 본문 16, 코드 13.

## 간격·형태·모션
- **간격**: 8px 그리드 (6·8·12·16). 섹션 간 16, 요소 간 8.
- **라운드**: 컨트롤 8 / 패널 12–14 / 팝오버 12 / 필(pill) 999.
- **테두리**: 1px, `--border`. 강조 필드는 `--border-strong`.
- **모션**: hover 120ms ease. 팝오버 fade+scale 120ms. 색상 펼치기 max-height 200ms.
  `prefers-reduced-motion` 존중(모션 제거).
- **포커스**: 항상 보이는 링 `outline:2px var(--accent)`.

## 컴포넌트·인터랙션 스펙
- **두 방향 버튼**: 만들기=액센트 채움(primary), 추출=아웃라인(ghost). 각 버튼에 화살표
  아이콘 + 캡션("마크다운 → Figma 프레임" / "선택 프레임 → 마크다운").
- **컨텍스트 "+"**: 입력창 라벨 우측. 입력창 편집(focus) 전엔 비활성(opacity .4), 편집 시 활성.
  클릭 → 팝오버: 구분선(즉시 삽입) / 표(옆 `행x열` 입력 + 삽입). 커서 위치에 삽입.
  Esc·바깥 클릭으로 닫힘.
- **표 규격**: `행x열`(예 `3x2`), 첫 행 항상 헤더. `×`도 허용. 상한 20행×10열.
- **설정 스트립**(하단, 조용): "문서 테마" 라벨 + [라이트|다크] 세그먼트 + "색상 ▶" 펼치기.
  색상(배경/글자 피커 + 미리보기)은 기본 접힘.

## 안티패턴 (피할 것)
- Win98식 회색 입체 버튼·기본 폼 컨트롤.
- 상시 노출되는 삽입 칩 무더기(메인을 어지럽힘).
- 액센트 남발(여러 색이 동시에 힘줌).
- 보라→파랑 그라데이션 히어로, 이모지 섹션 마커, 전부 가운데정렬 같은 AI 기본 룩.

## 참고
- 컨텍스트 삽입 메뉴: Notion `/`·`+` 블록 삽입 패턴.
- 점진 공개: Nielsen progressive disclosure (초기 완료 30–50% 빠름).
- 목업(인터랙티브): scratchpad `fig-md-panel-mockup.html` → Artifact.
