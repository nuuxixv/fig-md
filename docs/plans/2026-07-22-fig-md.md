# Fig.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 피그마 프레임을 노션 페이지처럼 쓰는 마크다운 라운드트립 에디터 플러그인(Fig.md) v1을 만든다 — 마크다운 투입→캔버스 편집→마크다운 출력.

**Architecture:** 순수 코어(마크다운 ↔ 내부 문서모델 `Doc`)와 피그마 계층(문서모델 ↔ 꼬리표 달린 노드 트리)을 분리한다. 코어는 Node에서 순수 함수로 테스트하고, 피그마 계층은 최소 가짜 API(`FakeFigma`)로 테스트한다. 출력은 임의 화면 역파싱이 아니라 `pluginData` 꼬리표를 따라 정확히 재조립한다.

**Tech Stack:** TypeScript, esbuild(번들), vitest(테스트). 런타임 외부 의존성 0(마크다운 파서 직접 구현). UI는 iframe HTML/CSS/TS.

## Global Constraints

- 런타임 의존성 0. 마크다운 파싱·직렬화는 직접 구현한다(markdown-it 등 미사용). devDependencies(typescript, esbuild, vitest, @figma/plugin-typings)만 허용.
- 위치: 저장소 루트(`fig-md/`). 독립 git repo로 관리한다.
- 문자열·주석·커밋 메시지의 한글은 실제 글자로 작성한다. `\uXXXX` 이스케이프 금지.
- 모든 코드/주석/식별자는 아래 "모듈 계약"의 이름을 정확히 그대로 쓴다.
- 커밋은 각 Task 마지막 스텝에서. 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` 붙인다.
- v1 마크다운 범위: 제목(#~###), 문단, 굵게`**`/기울임`*`/인라인코드`` ` ``/링크`[]()`, 목록(순서·비순서·2칸 들여쓰기 중첩·체크박스`- [ ]`), 인용`>`, 구분선`---`, 코드블록```` ``` ````, 표, 이미지`![]()`.
- v1 비목표: 토글 접기, 표 행/열 편집, 실시간 양방향 바인딩, 디자인시스템 토큰 동기화, 캔버스 손질 보존 재렌더(전체 재생성만).

---

## 모듈 계약 (모든 Task가 공유하는 이름·타입)

아래는 Task 전반에서 참조되는 확정 인터페이스다. 각 Task는 자기 파일만 보므로 여기서 이름을 익힌다.

```ts
// src/core/model.ts 가 export 하는 타입
export type Inline =
  | { type: 'text'; value: string }
  | { type: 'strong'; children: Inline[] }
  | { type: 'em'; children: Inline[] }
  | { type: 'code'; value: string }
  | { type: 'link'; href: string; children: Inline[] };

export interface Heading   { type: 'heading'; level: 1 | 2 | 3; inlines: Inline[]; }
export interface Paragraph { type: 'paragraph'; inlines: Inline[]; }
export interface Divider   { type: 'divider'; }
export interface CodeBlock { type: 'code'; lang: string; value: string; }
export interface ImageBlock{ type: 'image'; src: string; alt: string; }
export interface Quote     { type: 'quote'; children: Block[]; }
export interface ListItem  { inlines: Inline[]; checked: boolean | null; children: List[]; }
export interface List      { type: 'list'; ordered: boolean; items: ListItem[]; }
export interface Table     { type: 'table'; header: string[]; rows: string[][]; }
export type Block = Heading | Paragraph | Divider | CodeBlock | ImageBlock | Quote | List | Table;
export interface Doc { blocks: Block[]; }

// 인라인 평탄화(렌더용) — src/core/inline.ts
export interface Run { text: string; bold: boolean; italic: boolean; code: boolean; href: string | null; }
```

**모듈별 공개 함수 (시그니처 고정):**

| 파일 | 함수 |
|---|---|
| `core/model.ts` | 위 타입들 (값 없음) |
| `core/inline.ts` | `parseInlines(s: string): Inline[]`, `serializeInlines(xs: Inline[]): string`, `flattenInlines(xs: Inline[]): Run[]`, `runsToInlines(rs: Run[]): Inline[]` |
| `core/parse.ts` | `parseMarkdown(md: string): Doc` |
| `core/serialize.ts` | `serializeDoc(doc: Doc): string` |
| `core/tag.ts` | `NS='figmd'`, `setBlockTag(n, type, meta?)`, `getBlockTag(n): {type: string; meta: any} | null`, `setCellTag(n, r, c)`, `getCellTag(n): {r: number; c: number} | null` |
| `core/figma-like.ts` | `FigmaLike`, `SceneLike`, `FrameLike`, `TextLike` 인터페이스 |
| `core/render.ts` | `renderDoc(doc: Doc, figma: FigmaLike): Promise<FrameLike>` |
| `core/read.ts` | `readDoc(page: FrameLike): { doc: Doc; warnings: string[] }` |

---

## 파일 구조

```
fig-md/
  manifest.json
  package.json
  tsconfig.json
  esbuild.mjs
  vitest.config.ts
  src/
    code.ts                메인 스레드: UI 메시지 ↔ 문서 조작
    ui.html                패널 셸
    ui.ts                  패널 로직(입력·버튼·미리보기·클립보드)
    core/
      model.ts             문서모델 타입
      inline.ts            인라인 파싱/직렬화/평탄화
      parse.ts             md → Doc
      serialize.ts         Doc → md
      tag.ts               pluginData 꼬리표 헬퍼
      figma-like.ts        피그마 API 최소 인터페이스
      render.ts            Doc → 노드 트리(꼬리표 심기)
      read.ts              노드 트리 → Doc(+경고)
  test/
    fake-figma.ts          FigmaLike 테스트 더블
    inline.test.ts  parse.test.ts  serialize.test.ts
    roundtrip.test.ts  render-read.test.ts  integration.test.ts
  docs/specs/  docs/plans/
```

---

### Task 1: 프로젝트 스캐폴드

**Files:**
- Create: `package.json`, `tsconfig.json`, `esbuild.mjs`, `vitest.config.ts`, `manifest.json`, `src/code.ts`, `src/ui.html`, `test/smoke.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces: 빌드/테스트 파이프라인. 이후 모든 Task가 `npm test`, `npm run build` 사용.

- [ ] **Step 1: package.json 작성**

```json
{
  "name": "fig-md",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node esbuild.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
```

- [ ] **Step 2: tsconfig.json / vitest.config.ts / esbuild.mjs 작성**

`tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "es2020",
    "module": "esnext",
    "moduleResolution": "bundler",
    "strict": true,
    "typeRoots": ["./node_modules/@types", "./node_modules/@figma"],
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src", "test"]
}
```

`vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({ test: { environment: 'node', include: ['test/**/*.test.ts'] } });
```

`esbuild.mjs`:
```js
import { build } from 'esbuild';
await build({
  entryPoints: { code: 'src/code.ts' },
  bundle: true, target: 'es2017', format: 'iife',
  outfile: 'dist/code.js', logLevel: 'info',
});
console.log('built dist/code.js');
```

- [ ] **Step 3: manifest.json 과 UI/코드 최소 파일**

`manifest.json`:
```json
{
  "name": "Fig.md",
  "id": "fig-md-local",
  "api": "1.0.0",
  "main": "dist/code.js",
  "ui": "dist/ui.html",
  "editorType": ["figma"],
  "documentAccess": "dynamic-page"
}
```

`src/code.ts`:
```ts
figma.showUI(__html__, { width: 380, height: 560 });
```

`src/ui.html`:
```html
<!doctype html><html><body><h1>Fig.md</h1></body></html>
```

- [ ] **Step 4: 스모크 테스트**

`test/smoke.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
describe('smoke', () => { it('runs', () => { expect(1 + 1).toBe(2); }); });
```

- [ ] **Step 5: 설치·실행 검증**

Run: `npm install && npm test`
Expected: PASS (`smoke > runs`).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: 프로젝트 스캐폴드 (esbuild+vitest+manifest)"
```

---

### Task 2: 문서모델 타입 + 인라인 평탄화

**Files:**
- Create: `src/core/model.ts`, `src/core/inline.ts`, `test/inline.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces: `model.ts`의 모든 타입. `inline.ts`의 `flattenInlines`, `runsToInlines`. (파싱/직렬화는 Task 3에서 같은 파일에 추가.)

- [ ] **Step 1: model.ts 작성 (위 "모듈 계약"의 타입 블록 그대로)**

`src/core/model.ts` = 모듈 계약 섹션의 타입 정의 전체를 그대로 붙여넣는다(Run 포함).

- [ ] **Step 2: 실패 테스트 — 평탄화 왕복**

`test/inline.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { flattenInlines, runsToInlines } from '../src/core/inline';
import type { Inline } from '../src/core/model';

describe('flatten/runs roundtrip', () => {
  it('plain text', () => {
    const xs: Inline[] = [{ type: 'text', value: 'hello' }];
    const rs = flattenInlines(xs);
    expect(rs).toEqual([{ text: 'hello', bold: false, italic: false, code: false, href: null }]);
    expect(runsToInlines(rs)).toEqual(xs);
  });
  it('bold + link', () => {
    const xs: Inline[] = [
      { type: 'strong', children: [{ type: 'text', value: 'A' }] },
      { type: 'link', href: 'http://x', children: [{ type: 'text', value: 'B' }] },
    ];
    const rs = flattenInlines(xs);
    expect(rs).toEqual([
      { text: 'A', bold: true, italic: false, code: false, href: null },
      { text: 'B', bold: false, italic: false, code: false, href: 'http://x' },
    ]);
    expect(runsToInlines(rs)).toEqual(xs);
  });
});
```

- [ ] **Step 3: Run to verify it fails**

Run: `npx vitest run test/inline.test.ts`
Expected: FAIL ("flattenInlines is not a function").

- [ ] **Step 4: inline.ts 평탄화 구현**

`src/core/inline.ts` (이 Task 범위 부분만; 파싱/직렬화 함수는 Task 3에서 추가):
```ts
import type { Inline, Run } from './model';

export function flattenInlines(xs: Inline[]): Run[] {
  const out: Run[] = [];
  const walk = (node: Inline, s: { bold: boolean; italic: boolean; code: boolean; href: string | null }) => {
    switch (node.type) {
      case 'text': out.push({ text: node.value, ...s }); break;
      case 'code': out.push({ text: node.value, bold: s.bold, italic: s.italic, code: true, href: s.href }); break;
      case 'strong': node.children.forEach(c => walk(c, { ...s, bold: true })); break;
      case 'em': node.children.forEach(c => walk(c, { ...s, italic: true })); break;
      case 'link': node.children.forEach(c => walk(c, { ...s, href: node.href })); break;
    }
  };
  xs.forEach(x => walk(x, { bold: false, italic: false, code: false, href: null }));
  return out;
}

// Run[] → Inline[]: link > strong > em > code 우선순위로 감싼다(평탄화의 역).
export function runsToInlines(rs: Run[]): Inline[] {
  return rs.map(r => {
    let node: Inline = r.code ? { type: 'code', value: r.text } : { type: 'text', value: r.text };
    if (r.em == null) {} // no-op placeholder (see below)
    if (r.italic && !r.code) node = { type: 'em', children: [node] };
    if (r.bold && !r.code) node = { type: 'strong', children: [node] };
    if (r.href) node = { type: 'link', href: r.href, children: [node] };
    return node;
  });
}
```
주의: 위 `if (r.em == null) {}` 줄은 삭제한다(오타 방지용 표식이 아님). 최종 코드에는 넣지 말 것.

- [ ] **Step 5: Run to verify it passes**

Run: `npx vitest run test/inline.test.ts`
Expected: PASS. 실패 시 `runsToInlines`의 래핑 순서(link 바깥, code 안쪽)를 점검한다.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 문서모델 타입 + 인라인 평탄화 왕복"
```

---

### Task 3: 인라인 파싱/직렬화

**Files:**
- Modify: `src/core/inline.ts` (함수 2개 추가)
- Modify: `test/inline.test.ts` (케이스 추가)

**Interfaces:**
- Consumes: `Inline` (model.ts)
- Produces: `parseInlines(s: string): Inline[]`, `serializeInlines(xs: Inline[]): string`

- [ ] **Step 1: 실패 테스트 — 인라인 파싱/직렬화 왕복**

`test/inline.test.ts`에 추가:
```ts
import { parseInlines, serializeInlines } from '../src/core/inline';

describe('inline parse/serialize', () => {
  const cases = [
    'plain',
    '**bold**',
    '*italic*',
    'a `code` b',
    '[text](http://x)',
    'mix **b** and *i* and [l](http://y)',
  ];
  for (const md of cases) {
    it(`roundtrip: ${md}`, () => {
      expect(serializeInlines(parseInlines(md))).toBe(md);
    });
  }
  it('parses bold node', () => {
    expect(parseInlines('**b**')).toEqual([{ type: 'strong', children: [{ type: 'text', value: 'b' }] }]);
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/inline.test.ts`
Expected: FAIL ("parseInlines is not a function").

- [ ] **Step 3: parseInlines / serializeInlines 구현**

`src/core/inline.ts`에 추가:
```ts
// 인라인 토크나이저: `code`, **strong**, *em*, [text](href), 나머지는 text.
// 우선순위: 코드(백틱)를 먼저 떼고, 그다음 링크, 강조.
export function parseInlines(s: string): Inline[] {
  const out: Inline[] = [];
  let i = 0;
  let buf = '';
  const flush = () => { if (buf) { out.push({ type: 'text', value: buf }); buf = ''; } };
  while (i < s.length) {
    const c = s[i];
    if (c === '`') {
      const end = s.indexOf('`', i + 1);
      if (end > i) { flush(); out.push({ type: 'code', value: s.slice(i + 1, end) }); i = end + 1; continue; }
    }
    if (c === '[') {
      const close = s.indexOf('](', i);
      const paren = close > -1 ? s.indexOf(')', close) : -1;
      if (close > i && paren > close) {
        flush();
        const label = s.slice(i + 1, close);
        const href = s.slice(close + 2, paren);
        out.push({ type: 'link', href, children: parseInlines(label) });
        i = paren + 1; continue;
      }
    }
    if (c === '*' && s[i + 1] === '*') {
      const end = s.indexOf('**', i + 2);
      if (end > i) { flush(); out.push({ type: 'strong', children: parseInlines(s.slice(i + 2, end)) }); i = end + 2; continue; }
    }
    if (c === '*') {
      const end = s.indexOf('*', i + 1);
      if (end > i) { flush(); out.push({ type: 'em', children: parseInlines(s.slice(i + 1, end)) }); i = end + 1; continue; }
    }
    buf += c; i++;
  }
  flush();
  return out;
}

export function serializeInlines(xs: Inline[]): string {
  return xs.map(x => {
    switch (x.type) {
      case 'text': return x.value;
      case 'code': return '`' + x.value + '`';
      case 'strong': return '**' + serializeInlines(x.children) + '**';
      case 'em': return '*' + serializeInlines(x.children) + '*';
      case 'link': return '[' + serializeInlines(x.children) + '](' + x.href + ')';
    }
  }).join('');
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `npx vitest run test/inline.test.ts`
Expected: PASS (평탄화 케이스 포함 전부).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: 인라인 파싱/직렬화 왕복"
```

---

### Task 4: 블록 파싱 — 제목/문단/구분선

**Files:**
- Create: `src/core/parse.ts`, `test/parse.test.ts`

**Interfaces:**
- Consumes: `Doc`, `Block`, `parseInlines`
- Produces: `parseMarkdown(md: string): Doc` (이 Task는 heading/paragraph/divider만; 이후 Task가 같은 함수에 분기 추가)

- [ ] **Step 1: 실패 테스트**

`test/parse.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { parseMarkdown } from '../src/core/parse';

describe('parse blocks (basic)', () => {
  it('heading levels', () => {
    expect(parseMarkdown('# A').blocks[0]).toEqual({ type: 'heading', level: 1, inlines: [{ type: 'text', value: 'A' }] });
    expect(parseMarkdown('### C').blocks[0]).toEqual({ type: 'heading', level: 3, inlines: [{ type: 'text', value: 'C' }] });
  });
  it('divider', () => {
    expect(parseMarkdown('---').blocks[0]).toEqual({ type: 'divider' });
  });
  it('paragraph joins consecutive lines with space', () => {
    expect(parseMarkdown('one\ntwo').blocks[0]).toEqual({ type: 'paragraph', inlines: [{ type: 'text', value: 'one two' }] });
  });
  it('blank line separates paragraphs', () => {
    const d = parseMarkdown('a\n\nb');
    expect(d.blocks.length).toBe(2);
    expect(d.blocks[1]).toEqual({ type: 'paragraph', inlines: [{ type: 'text', value: 'b' }] });
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/parse.test.ts`
Expected: FAIL ("parseMarkdown is not a function").

- [ ] **Step 3: parse.ts 골격 + 기본 블록 구현**

`src/core/parse.ts`:
```ts
import type { Doc, Block } from './model';
import { parseInlines } from './inline';

export function parseMarkdown(md: string): Doc {
  const lines = md.replace(/\r\n?/g, '\n').split('\n');
  const blocks: Block[] = [];
  let i = 0;
  while (i < lines.length) {
    const line = lines[i];
    if (line.trim() === '') { i++; continue; }
    if (line.trim() === '---') { blocks.push({ type: 'divider' }); i++; continue; }
    const h = /^(#{1,3})\s+(.*)$/.exec(line);
    if (h) {
      blocks.push({ type: 'heading', level: h[1].length as 1 | 2 | 3, inlines: parseInlines(h[2].trim()) });
      i++; continue;
    }
    // paragraph: 연속된 비어있지 않은 "일반" 줄을 공백으로 합침
    const para: string[] = [];
    while (i < lines.length && lines[i].trim() !== '' && !isBlockStart(lines[i])) {
      para.push(lines[i].trim()); i++;
    }
    blocks.push({ type: 'paragraph', inlines: parseInlines(para.join(' ')) });
  }
  return { blocks };
}

// 이후 Task들이 여기에 자기 블록 시작 판별을 추가한다(목록/인용/표/코드/이미지).
function isBlockStart(line: string): boolean {
  const t = line.trim();
  return t === '---' || /^#{1,3}\s/.test(line);
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `npx vitest run test/parse.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: 블록 파싱 — 제목/문단/구분선"
```

---

### Task 5: 블록 직렬화 — 제목/문단/구분선 + 왕복 테스트

**Files:**
- Create: `src/core/serialize.ts`, `test/serialize.test.ts`, `test/roundtrip.test.ts`

**Interfaces:**
- Consumes: `Doc`, `Block`, `serializeInlines`
- Produces: `serializeDoc(doc: Doc): string`

- [ ] **Step 1: 실패 테스트 (직렬화 + 왕복)**

`test/serialize.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { serializeDoc } from '../src/core/serialize';

describe('serialize blocks (basic)', () => {
  it('heading', () => {
    expect(serializeDoc({ blocks: [{ type: 'heading', level: 2, inlines: [{ type: 'text', value: 'X' }] }] })).toBe('## X');
  });
  it('divider + paragraph separated by blank line', () => {
    const md = serializeDoc({ blocks: [
      { type: 'paragraph', inlines: [{ type: 'text', value: 'a' }] },
      { type: 'divider' },
      { type: 'paragraph', inlines: [{ type: 'text', value: 'b' }] },
    ]});
    expect(md).toBe('a\n\n---\n\nb');
  });
});
```

`test/roundtrip.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { parseMarkdown } from '../src/core/parse';
import { serializeDoc } from '../src/core/serialize';

const norm = (s: string) => s.replace(/\r\n?/g, '\n').trim();
export function assertRoundtrip(md: string) {
  expect(norm(serializeDoc(parseMarkdown(md)))).toBe(norm(md));
}

describe('roundtrip basic', () => {
  for (const md of ['# Title', '## Sub', 'hello world', 'a\n\n---\n\nb', 'para with **bold** and [l](http://x)']) {
    it(`md → Doc → md: ${md}`, () => assertRoundtrip(md));
  }
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/serialize.test.ts test/roundtrip.test.ts`
Expected: FAIL ("serializeDoc is not a function").

- [ ] **Step 3: serialize.ts 골격 + 기본 블록 구현**

`src/core/serialize.ts`:
```ts
import type { Doc, Block } from './model';
import { serializeInlines } from './inline';

export function serializeDoc(doc: Doc): string {
  return doc.blocks.map(serializeBlock).join('\n\n');
}

// 이후 Task들이 여기에 자기 블록 분기를 추가한다.
export function serializeBlock(b: Block): string {
  switch (b.type) {
    case 'heading': return '#'.repeat(b.level) + ' ' + serializeInlines(b.inlines);
    case 'paragraph': return serializeInlines(b.inlines);
    case 'divider': return '---';
    default: return ''; // 다른 타입은 후속 Task에서 채움
  }
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `npx vitest run test/serialize.test.ts test/roundtrip.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: 블록 직렬화 — 제목/문단/구분선 + 왕복"
```

---

### Task 6: 목록 (순서/비순서/중첩/체크박스)

**Files:**
- Modify: `src/core/parse.ts`, `src/core/serialize.ts`, `test/roundtrip.test.ts`

**Interfaces:**
- Consumes: `List`, `ListItem`
- Produces: parse/serialize의 목록 분기. 중첩은 들여쓰기 2칸/레벨.

- [ ] **Step 1: 실패 테스트 (왕복 케이스 추가)**

`test/roundtrip.test.ts`의 배열에 추가:
```ts
describe('roundtrip lists', () => {
  const cases = [
    '- a\n- b',
    '1. one\n2. two',
    '- top\n  - nested\n  - nested2',
    '- [ ] todo\n- [x] done',
  ];
  for (const md of cases) { it(md, () => assertRoundtrip(md)); }
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/roundtrip.test.ts`
Expected: FAIL (목록이 문단으로 파싱됨).

- [ ] **Step 3: parse.ts 에 목록 파싱 추가**

`isBlockStart`에 목록 판별 추가하고, 메인 루프에서 목록 블록을 처리한다. `parse.ts`에 함수 추가:
```ts
// isBlockStart 교체
function isBlockStart(line: string): boolean {
  const t = line.trim();
  return t === '---' || /^#{1,3}\s/.test(line) || isListItem(line);
}
function isListItem(line: string): boolean {
  return /^(\s*)([-*]|\d+\.)\s+/.test(line);
}
function indentOf(line: string): number {
  const m = /^(\s*)/.exec(line)!; return Math.floor(m[1].length / 2);
}
```
메인 while 루프에서 `if (h) {...}` 다음, paragraph 처리 앞에 추가:
```ts
    if (isListItem(line)) {
      const [list, next] = parseList(lines, i, indentOf(line));
      blocks.push(list); i = next; continue;
    }
```
그리고 재귀 목록 파서:
```ts
import type { List, ListItem } from './model';

function parseList(lines: string[], start: number, level: number): [List, number] {
  const first = /^(\s*)([-*]|\d+\.)\s+(.*)$/.exec(lines[start])!;
  const ordered = /\d+\./.test(first[2]);
  const items: ListItem[] = [];
  let i = start;
  while (i < lines.length) {
    const m = /^(\s*)([-*]|\d+\.)\s+(.*)$/.exec(lines[i]);
    if (!m || lines[i].trim() === '') break;
    const lvl = indentOf(lines[i]);
    if (lvl < level) break;
    if (lvl > level) { // 하위 목록은 직전 항목의 children으로
      const [child, next] = parseList(lines, i, lvl);
      if (items.length) items[items.length - 1].children.push(child);
      i = next; continue;
    }
    let text = m[3];
    let checked: boolean | null = null;
    const cb = /^\[( |x)\]\s+(.*)$/.exec(text);
    if (cb) { checked = cb[1] === 'x'; text = cb[2]; }
    items.push({ inlines: parseInlines(text), checked, children: [] });
    i++;
  }
  return [{ type: 'list', ordered, items }, i];
}
```

- [ ] **Step 4: serialize.ts 에 목록 직렬화 추가**

`serializeBlock`의 switch에 case 추가 + 헬퍼:
```ts
import type { List } from './model';
// switch 안:
    case 'list': return serializeList(b, 0);
// 파일 하단:
function serializeList(list: List, level: number): string {
  const pad = '  '.repeat(level);
  const lines: string[] = [];
  list.items.forEach((it, idx) => {
    const marker = list.ordered ? `${idx + 1}.` : '-';
    const box = it.checked === null ? '' : it.checked ? '[x] ' : '[ ] ';
    lines.push(`${pad}${marker} ${box}${serializeInlines(it.inlines)}`);
    it.children.forEach(child => lines.push(serializeList(child, level + 1)));
  });
  return lines.join('\n');
}
```

- [ ] **Step 5: Run to verify it passes**

Run: `npx vitest run test/roundtrip.test.ts`
Expected: PASS (목록 4케이스 포함).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 목록 파싱/직렬화 (순서·비순서·중첩·체크박스)"
```

---

### Task 7: 인용/콜아웃 (`>`)

**Files:**
- Modify: `src/core/parse.ts`, `src/core/serialize.ts`, `test/roundtrip.test.ts`

**Interfaces:**
- Consumes: `Quote`
- Produces: parse/serialize의 인용 분기. 인용 내부는 문단만(v1).

- [ ] **Step 1: 실패 테스트**

`test/roundtrip.test.ts`에 추가:
```ts
describe('roundtrip quote', () => {
  for (const md of ['> a line', '> line one\n> line two']) { it(md, () => assertRoundtrip(md)); }
});
```
주의: `> line one\n> line two`는 각 줄이 별도 문단이 되도록 직렬화한다(아래 구현과 일치). 왕복 시 동일 문자열 유지.

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/roundtrip.test.ts`
Expected: FAIL (인용이 문단으로 파싱됨).

- [ ] **Step 3: parse.ts 인용 추가**

`isBlockStart`에 `|| /^>\s?/.test(line)` 추가. 메인 루프 목록 처리 뒤에:
```ts
    if (/^>\s?/.test(line)) {
      const inner: string[] = [];
      while (i < lines.length && /^>\s?/.test(lines[i])) { inner.push(lines[i].replace(/^>\s?/, '')); i++; }
      const children = inner
        .filter(t => t.trim() !== '')
        .map(t => ({ type: 'paragraph' as const, inlines: parseInlines(t.trim()) }));
      blocks.push({ type: 'quote', children }); continue;
    }
```
(import에 `Quote`는 타입 추론으로 불필요. `Block` 이미 import.)

- [ ] **Step 4: serialize.ts 인용 추가**

`serializeBlock` switch에:
```ts
    case 'quote':
      return b.children.map(c => '> ' + serializeBlock(c)).join('\n');
```

- [ ] **Step 5: Run to verify it passes**

Run: `npx vitest run test/roundtrip.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 인용/콜아웃 파싱/직렬화"
```

---

### Task 8: 코드블록 + 이미지

**Files:**
- Modify: `src/core/parse.ts`, `src/core/serialize.ts`, `test/roundtrip.test.ts`

**Interfaces:**
- Consumes: `CodeBlock`, `ImageBlock`
- Produces: parse/serialize의 코드블록·이미지 분기.

- [ ] **Step 1: 실패 테스트**

`test/roundtrip.test.ts`에 추가:
```ts
describe('roundtrip code + image', () => {
  for (const md of ['```js\nconst a = 1;\n```', '```\nplain\n```', '![alt](http://img/a.png)']) {
    it(md, () => assertRoundtrip(md));
  }
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/roundtrip.test.ts`
Expected: FAIL.

- [ ] **Step 3: parse.ts 코드블록·이미지 추가**

메인 루프 상단(divider 판별 뒤, heading 앞)에 코드펜스 처리:
```ts
    const fence = /^```(.*)$/.exec(line.trim());
    if (fence) {
      const lang = fence[1].trim();
      const body: string[] = []; i++;
      while (i < lines.length && lines[i].trim() !== '```') { body.push(lines[i]); i++; }
      i++; // 닫는 펜스 소비
      blocks.push({ type: 'code', lang, value: body.join('\n') }); continue;
    }
```
이미지(단독 줄) — heading 판별 뒤에:
```ts
    const img = /^!\[([^\]]*)\]\(([^)]*)\)$/.exec(line.trim());
    if (img) { blocks.push({ type: 'image', alt: img[1], src: img[2] }); i++; continue; }
```
`isBlockStart`에 `|| line.trim().startsWith('```') || /^!\[[^\]]*\]\([^)]*\)$/.test(line.trim())` 추가.

- [ ] **Step 4: serialize.ts 코드블록·이미지 추가**

`serializeBlock` switch에:
```ts
    case 'code': return '```' + b.lang + '\n' + b.value + '\n```';
    case 'image': return `![${b.alt}](${b.src})`;
```

- [ ] **Step 5: Run to verify it passes**

Run: `npx vitest run test/roundtrip.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 코드블록 + 이미지 파싱/직렬화"
```

---

### Task 9: 표 (파싱/직렬화)

**Files:**
- Modify: `src/core/parse.ts`, `src/core/serialize.ts`, `test/roundtrip.test.ts`

**Interfaces:**
- Consumes: `Table`
- Produces: parse/serialize의 표 분기. 셀은 평문 문자열(v1).

- [ ] **Step 1: 실패 테스트**

`test/roundtrip.test.ts`에 추가:
```ts
describe('roundtrip table', () => {
  const md = '| A | B |\n| --- | --- |\n| 1 | 2 |\n| 3 | 4 |';
  it('2x2', () => assertRoundtrip(md));
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/roundtrip.test.ts`
Expected: FAIL.

- [ ] **Step 3: parse.ts 표 파싱 추가**

헬퍼 + 메인 루프 분기(이미지 판별 뒤):
```ts
function splitRow(line: string): string[] {
  return line.trim().replace(/^\|/, '').replace(/\|$/, '').split('|').map(c => c.trim());
}
function isTableStart(lines: string[], i: number): boolean {
  return /\|/.test(lines[i] ?? '') && /^\s*\|?\s*:?-{1,}/.test(lines[i + 1] ?? '');
}
```
메인 루프에:
```ts
    if (isTableStart(lines, i)) {
      const header = splitRow(lines[i]); i += 2; // 헤더 + 구분선 소비
      const rows: string[][] = [];
      while (i < lines.length && /\|/.test(lines[i]) && lines[i].trim() !== '') { rows.push(splitRow(lines[i])); i++; }
      blocks.push({ type: 'table', header, rows }); continue;
    }
```
`isBlockStart`에 표는 다음 줄까지 봐야 하므로 넣지 않는다(문단 병합이 표 헤더를 삼키지 않도록, 메인 루프에서 표 분기를 문단 처리보다 앞에 둔다 — 위 코드가 이미 그러함).

- [ ] **Step 4: serialize.ts 표 직렬화 추가**

`serializeBlock` switch에:
```ts
    case 'table': {
      const head = '| ' + b.header.join(' | ') + ' |';
      const sep = '| ' + b.header.map(() => '---').join(' | ') + ' |';
      const body = b.rows.map(r => '| ' + r.join(' | ') + ' |').join('\n');
      return [head, sep, body].filter(Boolean).join('\n');
    }
```

- [ ] **Step 5: Run to verify it passes**

Run: `npx vitest run test/roundtrip.test.ts`
Expected: PASS. 전체 코어 테스트도 확인: `npx vitest run` → 전부 PASS.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 표 파싱/직렬화 (v1 평문 셀)"
```

---

### Task 10: 피그마 최소 인터페이스 + FakeFigma + 꼬리표 헬퍼

**Files:**
- Create: `src/core/figma-like.ts`, `src/core/tag.ts`, `test/fake-figma.ts`, `test/tag.test.ts`

**Interfaces:**
- Consumes: 없음(피그마 API 서브셋 정의)
- Produces: `FigmaLike` 등 인터페이스, `FakeFigma`, `tag.ts` 헬퍼.

- [ ] **Step 1: figma-like.ts 작성 (render/read 가 쓰는 API 서브셋만)**

`src/core/figma-like.ts`:
```ts
export interface Paint { type: 'SOLID'; color: { r: number; g: number; b: number }; }
export interface FontName { family: string; style: string; }

export interface BaseLike {
  type: string;
  name: string;
  setPluginData(key: string, value: string): void;
  getPluginData(key: string): string;
  remove(): void;
}
export interface TextLike extends BaseLike {
  type: 'TEXT';
  characters: string;
  fontName: FontName;
  fontSize: number;
  setRangeFontName(start: number, end: number, font: FontName): void;
  setRangeFills(start: number, end: number, fills: Paint[]): void;
  setRangeTextDecoration(start: number, end: number, deco: 'NONE' | 'UNDERLINE'): void;
  getRangeFontName(start: number, end: number): FontName | symbol;
  getRangeFills(start: number, end: number): Paint[] | symbol;
  getRangeHyperlink(start: number, end: number): { type: 'URL'; value: string } | null | symbol;
  setRangeHyperlink(start: number, end: number, link: { type: 'URL'; value: string } | null): void;
}
export interface FrameLike extends BaseLike {
  type: 'FRAME';
  layoutMode: 'NONE' | 'VERTICAL' | 'HORIZONTAL';
  itemSpacing: number;
  paddingTop: number; paddingBottom: number; paddingLeft: number; paddingRight: number;
  children: SceneLike[];
  appendChild(n: SceneLike): void;
}
export type SceneLike = FrameLike | TextLike;

export interface FigmaLike {
  createFrame(): FrameLike;
  createText(): TextLike;
  loadFontAsync(font: FontName): Promise<void>;
}
export const MIXED: symbol = Symbol('mixed');
```

- [ ] **Step 2: 실패 테스트 — 꼬리표 왕복**

`test/tag.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { setBlockTag, getBlockTag, setCellTag, getCellTag } from '../src/core/tag';
import { FakeFigma } from './fake-figma';

describe('tag helpers', () => {
  it('block tag roundtrip', () => {
    const f = new FakeFigma();
    const n = f.createFrame();
    setBlockTag(n, 'heading', { level: 2 });
    expect(getBlockTag(n)).toEqual({ type: 'heading', meta: { level: 2 } });
  });
  it('no tag returns null', () => {
    const f = new FakeFigma();
    expect(getBlockTag(f.createFrame())).toBeNull();
  });
  it('cell tag roundtrip', () => {
    const f = new FakeFigma();
    const n = f.createText();
    setCellTag(n, 1, 2);
    expect(getCellTag(n)).toEqual({ r: 1, c: 2 });
  });
});
```

- [ ] **Step 3: FakeFigma 구현**

`test/fake-figma.ts`:
```ts
import type { FigmaLike, FrameLike, TextLike, SceneLike, FontName, Paint } from '../src/core/figma-like';
import { MIXED } from '../src/core/figma-like';

class Base {
  name = ''; data: Record<string, string> = {};
  setPluginData(k: string, v: string) { this.data[k] = v; }
  getPluginData(k: string) { return this.data[k] ?? ''; }
  remove() {}
}
class FakeText extends Base implements TextLike {
  type = 'TEXT' as const;
  characters = '';
  fontName: FontName = { family: 'Inter', style: 'Regular' };
  fontSize = 16;
  fonts: FontName[] = []; fills: Paint[][] = []; links: ({ type: 'URL'; value: string } | null)[] = [];
  private ensure(n: number) {
    while (this.fonts.length < n) this.fonts.push(this.fontName);
    while (this.fills.length < n) this.fills.push([{ type: 'SOLID', color: { r: 0, g: 0, b: 0 } }]);
    while (this.links.length < n) this.links.push(null);
  }
  setRangeFontName(s: number, e: number, f: FontName) { this.ensure(e); for (let i = s; i < e; i++) this.fonts[i] = f; }
  setRangeFills(s: number, e: number, f: Paint[]) { this.ensure(e); for (let i = s; i < e; i++) this.fills[i] = f; }
  setRangeTextDecoration() {}
  setRangeHyperlink(s: number, e: number, l: { type: 'URL'; value: string } | null) { this.ensure(e); for (let i = s; i < e; i++) this.links[i] = l; }
  getRangeFontName(s: number, e: number) { this.ensure(e); const first = this.fonts[s]; for (let i = s; i < e; i++) if (this.fonts[i].style !== first.style) return MIXED; return first; }
  getRangeFills(s: number, e: number) { this.ensure(e); return this.fills[s]; }
  getRangeHyperlink(s: number, e: number) { this.ensure(e); const first = this.links[s]; for (let i = s; i < e; i++) if ((this.links[i]?.value ?? null) !== (first?.value ?? null)) return MIXED; return first; }
}
class FakeFrame extends Base implements FrameLike {
  type = 'FRAME' as const;
  layoutMode: 'NONE' | 'VERTICAL' | 'HORIZONTAL' = 'NONE';
  itemSpacing = 0; paddingTop = 0; paddingBottom = 0; paddingLeft = 0; paddingRight = 0;
  children: SceneLike[] = [];
  appendChild(n: SceneLike) { this.children.push(n); }
}
export class FakeFigma implements FigmaLike {
  createFrame() { return new FakeFrame(); }
  createText() { return new FakeText(); }
  async loadFontAsync() {}
}
```

- [ ] **Step 4: tag.ts 구현**

`src/core/tag.ts`:
```ts
import type { BaseLike } from './figma-like';

export const NS = 'figmd';
const BLOCK = `${NS}:block`;
const META = `${NS}:meta`;
const CELL = `${NS}:cell`;

export function setBlockTag(n: BaseLike, type: string, meta: any = {}): void {
  n.setPluginData(BLOCK, type);
  n.setPluginData(META, JSON.stringify(meta));
}
export function getBlockTag(n: BaseLike): { type: string; meta: any } | null {
  const type = n.getPluginData(BLOCK);
  if (!type) return null;
  const raw = n.getPluginData(META);
  return { type, meta: raw ? JSON.parse(raw) : {} };
}
export function setCellTag(n: BaseLike, r: number, c: number): void {
  n.setPluginData(CELL, JSON.stringify({ r, c }));
}
export function getCellTag(n: BaseLike): { r: number; c: number } | null {
  const raw = n.getPluginData(CELL);
  return raw ? JSON.parse(raw) : null;
}
```

- [ ] **Step 5: Run to verify it passes**

Run: `npx vitest run test/tag.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 피그마 최소 인터페이스 + FakeFigma + 꼬리표 헬퍼"
```

---

### Task 11: 렌더 (Doc → 노드) + 읽기 (노드 → Doc) 블록 골격

**Files:**
- Create: `src/core/render.ts`, `src/core/read.ts`, `test/render-read.test.ts`

**Interfaces:**
- Consumes: `Doc`, `FigmaLike`, `FrameLike`, tag 헬퍼, inline 평탄화
- Produces: `renderDoc(doc, figma): Promise<FrameLike>`, `readDoc(page): { doc; warnings }`. 이 Task는 heading/paragraph/divider/code/image/quote/list까지(표는 Task 13).

- [ ] **Step 1: 실패 테스트 — 렌더→읽기 왕복 (표·인라인 제외)**

`test/render-read.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { parseMarkdown } from '../src/core/parse';
import { serializeDoc } from '../src/core/serialize';
import { renderDoc } from '../src/core/render';
import { readDoc } from '../src/core/read';
import { FakeFigma } from './fake-figma';

const norm = (s: string) => s.replace(/\r\n?/g, '\n').trim();
async function nodeRoundtrip(md: string) {
  const doc = parseMarkdown(md);
  const page = await renderDoc(doc, new FakeFigma());
  const { doc: back, warnings } = readDoc(page);
  expect(warnings).toEqual([]);
  expect(norm(serializeDoc(back))).toBe(norm(md));
}

describe('render→read roundtrip (no table/inline styling)', () => {
  for (const md of [
    '# Title',
    'plain paragraph',
    '---',
    '- a\n- b\n  - c',
    '1. one\n2. two',
    '- [ ] todo\n- [x] done',
    '> a quote',
    '```js\nconst a = 1;\n```',
    '![alt](http://img/a.png)',
  ]) { it(md, () => nodeRoundtrip(md)); }
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/render-read.test.ts`
Expected: FAIL ("renderDoc is not a function").

- [ ] **Step 3: render.ts 구현 (블록 골격)**

`src/core/render.ts`:
```ts
import type { Doc, Block, List } from './model';
import type { FigmaLike, FrameLike, TextLike, FontName } from './figma-like';
import { setBlockTag } from './tag';
import { flattenInlines } from './inline';

const REGULAR: FontName = { family: 'Inter', style: 'Regular' };
const BOLD: FontName = { family: 'Inter', style: 'Bold' };
const MONO: FontName = { family: 'Roboto Mono', style: 'Regular' };
const HEADING_SIZE = { 1: 32, 2: 24, 3: 19 } as const;

export async function renderDoc(doc: Doc, figma: FigmaLike): Promise<FrameLike> {
  await figma.loadFontAsync(REGULAR);
  await figma.loadFontAsync(BOLD);
  await figma.loadFontAsync(MONO);
  const page = figma.createFrame();
  page.name = 'Fig.md Page';
  page.layoutMode = 'VERTICAL';
  page.itemSpacing = 12;
  page.paddingTop = page.paddingBottom = page.paddingLeft = page.paddingRight = 24;
  setBlockTag(page, 'page', { version: '1' });
  for (const b of doc.blocks) page.appendChild(renderBlock(b, figma));
  return page;
}

function textFromInlines(figma: FigmaLike, inlines: Block extends never ? never : any): TextLike {
  const t = figma.createText();
  const runs = flattenInlines(inlines);
  t.characters = runs.map(r => r.text).join('');
  return t;
}

function renderBlock(b: Block, figma: FigmaLike): FrameLike | TextLike {
  switch (b.type) {
    case 'heading': {
      const t = textFromInlines(figma, b.inlines);
      t.fontName = BOLD; t.fontSize = HEADING_SIZE[b.level];
      setBlockTag(t, 'heading', { level: b.level });
      return t;
    }
    case 'paragraph': {
      const t = textFromInlines(figma, b.inlines);
      setBlockTag(t, 'paragraph', {});
      return t;
    }
    case 'divider': {
      const f = figma.createFrame(); f.name = 'divider';
      setBlockTag(f, 'divider', {});
      return f;
    }
    case 'code': {
      const t = figma.createText(); t.characters = b.value; t.fontName = MONO; t.fontSize = 13;
      setBlockTag(t, 'code', { lang: b.lang });
      return t;
    }
    case 'image': {
      const f = figma.createFrame(); f.name = `image: ${b.alt || b.src}`;
      setBlockTag(f, 'image', { src: b.src, alt: b.alt });
      return f;
    }
    case 'quote': {
      const f = figma.createFrame(); f.layoutMode = 'VERTICAL'; f.itemSpacing = 4;
      setBlockTag(f, 'quote', {});
      b.children.forEach(c => f.appendChild(renderBlock(c, figma)));
      return f;
    }
    case 'list': return renderList(b, figma);
    case 'table': { const f = figma.createFrame(); setBlockTag(f, 'table', { rows: 0, cols: 0 }); return f; } // Task 13에서 채움
  }
}

function renderList(list: List, figma: FigmaLike): FrameLike {
  const f = figma.createFrame(); f.layoutMode = 'VERTICAL'; f.itemSpacing = 4;
  setBlockTag(f, 'list', { ordered: list.ordered });
  list.items.forEach((it, idx) => {
    const t = figma.createText();
    const marker = list.ordered ? `${idx + 1}.` : '•';
    const box = it.checked === null ? '' : it.checked ? '[x] ' : '[ ] ';
    t.characters = `${marker} ${box}${flattenInlines(it.inlines).map(r => r.text).join('')}`;
    setBlockTag(t, 'list-item', { ordered: list.ordered, index: idx, checked: it.checked });
    f.appendChild(t);
    it.children.forEach(child => f.appendChild(renderList(child, figma)));
  });
  return f;
}
```
주의: `textFromInlines`의 타입 표기 `Block extends never ? never : any`는 잘못된 표식이다. 실제로는 `inlines: import('./model').Inline[]` 로 선언한다:
```ts
import type { Inline } from './model';
function textFromInlines(figma: FigmaLike, inlines: Inline[]): TextLike { /* 위 본문 동일 */ }
```

- [ ] **Step 4: read.ts 구현 (블록 골격)**

`src/core/read.ts`:
```ts
import type { Doc, Block, List, ListItem } from './model';
import type { FrameLike, SceneLike, TextLike } from './figma-like';
import { getBlockTag } from './tag';
import { parseInlines } from './inline';

export function readDoc(page: FrameLike): { doc: Doc; warnings: string[] } {
  const warnings: string[] = [];
  const blocks: Block[] = [];
  for (const child of page.children) {
    const b = readBlock(child, warnings);
    if (b) blocks.push(b);
  }
  return { doc: { blocks }, warnings };
}

function stripMarker(s: string): { text: string; checked: boolean | null } {
  let text = s.replace(/^(\d+\.|•|-)\s+/, '');
  let checked: boolean | null = null;
  const cb = /^\[( |x)\]\s+(.*)$/.exec(text);
  if (cb) { checked = cb[1] === 'x'; text = cb[2]; }
  return { text, checked };
}

function readBlock(node: SceneLike, warnings: string[]): Block | null {
  const tag = getBlockTag(node);
  if (!tag) { warnings.push(`꼬리표 없는 노드 무시: "${node.name || node.type}"`); return null; }
  const t = node as TextLike; const f = node as FrameLike;
  switch (tag.type) {
    case 'heading': return { type: 'heading', level: tag.meta.level, inlines: parseInlines(t.characters) };
    case 'paragraph': return { type: 'paragraph', inlines: parseInlines(t.characters) };
    case 'divider': return { type: 'divider' };
    case 'code': return { type: 'code', lang: tag.meta.lang ?? '', value: t.characters };
    case 'image': return { type: 'image', src: tag.meta.src ?? '', alt: tag.meta.alt ?? '' };
    case 'quote': {
      const children = f.children.map(c => readBlock(c, warnings)).filter((x): x is Block => !!x);
      return { type: 'quote', children };
    }
    case 'list': return readList(f, warnings);
    case 'table': return { type: 'table', header: [], rows: [] }; // Task 13에서 채움
    default: warnings.push(`알 수 없는 블록 타입 무시: ${tag.type}`); return null;
  }
}

function readList(frame: FrameLike, warnings: string[]): List {
  const ordered = !!getBlockTag(frame)?.meta.ordered;
  const items: ListItem[] = [];
  for (const child of frame.children) {
    const tag = getBlockTag(child);
    if (tag?.type === 'list-item') {
      const { text, checked } = stripMarker((child as TextLike).characters);
      items.push({ inlines: parseInlines(text), checked, children: [] });
    } else if (tag?.type === 'list') {
      const sub = readList(child as FrameLike, warnings);
      if (items.length) items[items.length - 1].children.push(sub);
    }
  }
  return { type: 'list', ordered, items };
}
```

- [ ] **Step 5: Run to verify it passes**

Run: `npx vitest run test/render-read.test.ts`
Expected: PASS (표·인라인 스타일 제외 케이스 전부).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 렌더/읽기 블록 골격 (표·인라인 제외)"
```

---

### Task 12: 인라인 문자 스타일 렌더/읽기

**Files:**
- Modify: `src/core/render.ts`, `src/core/read.ts`, `test/render-read.test.ts`

**Interfaces:**
- Consumes: `Run`, `flattenInlines`, `runsToInlines`, TextLike range API
- Produces: 텍스트 노드의 문자 범위 스타일 렌더 + 읽기.

- [ ] **Step 1: 실패 테스트 — 인라인 스타일 왕복**

`test/render-read.test.ts`에 추가:
```ts
describe('render→read inline styling', () => {
  for (const md of [
    'para with **bold** word',
    'has *italic* here',
    'link [go](http://x) end',
    'mix **b** and *i* and `c`',
  ]) { it(md, () => nodeRoundtrip(md)); }
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/render-read.test.ts`
Expected: FAIL (bold/link 등이 평문으로 왕복되어 `**` 소실).

- [ ] **Step 3: render.ts — 문자 범위 스타일 적용**

`textFromInlines`를 교체(범위 스타일 적용):
```ts
import type { Run } from './model';
const BLUE = [{ type: 'SOLID' as const, color: { r: 0.1, g: 0.4, b: 0.9 } }];
const BLACK = [{ type: 'SOLID' as const, color: { r: 0, g: 0, b: 0 } }];

function applyRuns(t: TextLike, runs: Run[]): void {
  let pos = 0;
  for (const r of runs) {
    const start = pos, end = pos + r.text.length;
    if (end > start) {
      const style = r.bold ? (r.italic ? 'Bold Italic' : 'Bold') : (r.italic ? 'Italic' : 'Regular');
      t.setRangeFontName(start, end, r.code ? { family: 'Roboto Mono', style: 'Regular' } : { family: 'Inter', style });
      t.setRangeFills(start, end, r.href ? BLUE : BLACK);
      t.setRangeHyperlink(start, end, r.href ? { type: 'URL', value: r.href } : null);
    }
    pos = end;
  }
}

function textFromInlines(figma: FigmaLike, inlines: Inline[]): TextLike {
  const t = figma.createText();
  const runs = flattenInlines(inlines);
  t.characters = runs.map(r => r.text).join('');
  applyRuns(t, runs);
  return t;
}
```
코드 스타일 감지를 위해 read에서 fontName.family로 code를 판별하므로, code run은 위에서 Mono로 지정된다.

- [ ] **Step 4: read.ts — 문자 범위 스타일에서 Run 복원**

`read.ts`에 헬퍼 추가 + heading/paragraph에서 사용:
```ts
import type { Run } from './model';
import { runsToInlines } from './inline';
import { MIXED } from './figma-like';

function readInlines(t: TextLike): ReturnType<typeof parseInlines> {
  const s = t.characters;
  if (s.length === 0) return [];
  const runs: Run[] = [];
  let start = 0;
  const styleAt = (i: number) => {
    const font = t.getRangeFontName(i, i + 1);
    const link = t.getRangeHyperlink(i, i + 1);
    const fam = (font as any).family as string;
    const sty = (font as any).style as string;
    const href = link && link !== MIXED ? (link as any).value as string : null;
    return {
      code: fam === 'Roboto Mono',
      bold: /Bold/.test(sty),
      italic: /Italic/.test(sty),
      href,
    };
  };
  let cur = styleAt(0);
  for (let i = 1; i <= s.length; i++) {
    const next = i < s.length ? styleAt(i) : null;
    const changed = !next || next.code !== cur.code || next.bold !== cur.bold || next.italic !== cur.italic || next.href !== cur.href;
    if (changed) {
      runs.push({ text: s.slice(start, i), ...cur });
      start = i; if (next) cur = next;
    }
  }
  return runsToInlines(runs);
}
```
그리고 `readBlock`의 heading/paragraph를 교체:
```ts
    case 'heading': return { type: 'heading', level: tag.meta.level, inlines: readInlines(t) };
    case 'paragraph': return { type: 'paragraph', inlines: readInlines(t) };
```

- [ ] **Step 5: Run to verify it passes**

Run: `npx vitest run test/render-read.test.ts`
Expected: PASS. 실패 시 FakeText의 `getRangeFontName`이 문자별 style을 반환하는지 확인.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 인라인 문자 스타일 렌더/읽기 왕복"
```

---

### Task 13: 표 렌더/읽기

**Files:**
- Modify: `src/core/render.ts`, `src/core/read.ts`, `test/render-read.test.ts`

**Interfaces:**
- Consumes: `Table`, `setCellTag`, `getCellTag`
- Produces: 표 블록의 노드 렌더 + 읽기(셀 꼬리표 기반 재조립).

- [ ] **Step 1: 실패 테스트**

`test/render-read.test.ts`에 추가:
```ts
describe('render→read table', () => {
  it('2x2', () => nodeRoundtrip('| A | B |\n| --- | --- |\n| 1 | 2 |'));
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `npx vitest run test/render-read.test.ts`
Expected: FAIL (Task 11의 표 스텁이 빈 표 반환).

- [ ] **Step 3: render.ts — 표 렌더 교체**

`renderBlock`의 `case 'table'`을 교체 + 헬퍼:
```ts
import { setCellTag } from './tag';

function renderTable(b: import('./model').Table, figma: FigmaLike): FrameLike {
  const grid = figma.createFrame(); grid.layoutMode = 'VERTICAL'; grid.itemSpacing = 1;
  const allRows = [b.header, ...b.rows];
  setBlockTag(grid, 'table', { rows: allRows.length, cols: b.header.length });
  allRows.forEach((row, r) => {
    const rowFrame = figma.createFrame(); rowFrame.layoutMode = 'HORIZONTAL'; rowFrame.itemSpacing = 1;
    rowFrame.name = `row ${r}`;
    row.forEach((cell, c) => {
      const t = figma.createText(); t.characters = cell;
      if (r === 0) t.fontName = { family: 'Inter', style: 'Bold' };
      setCellTag(t, r, c);
      rowFrame.appendChild(t);
    });
    grid.appendChild(rowFrame);
  });
  return grid;
}
```
`case 'table': return renderTable(b, figma);`

- [ ] **Step 4: read.ts — 표 읽기 교체**

`readBlock`의 `case 'table'`을 교체 + 헬퍼:
```ts
import { getCellTag } from './tag';

function readTable(grid: FrameLike): import('./model').Table {
  const meta = getBlockTag(grid)!.meta;
  const cols: number = meta.cols;
  const cells: string[][] = [];
  for (const rowFrame of grid.children) {
    const row: string[] = [];
    for (const cell of (rowFrame as FrameLike).children) {
      const ct = getCellTag(cell);
      if (ct) row[ct.c] = (cell as TextLike).characters;
    }
    for (let c = 0; c < cols; c++) if (row[c] === undefined) row[c] = '';
    cells.push(row);
  }
  const header = cells[0] ?? [];
  const rows = cells.slice(1);
  return { type: 'table', header, rows };
}
```
`case 'table': return readTable(f);`

- [ ] **Step 5: Run to verify it passes**

Run: `npx vitest run`
Expected: 전체 PASS (코어 + 렌더/읽기 + 표).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: 표 렌더/읽기 (셀 꼬리표 재조립)"
```

---

### Task 14: 전체 라운드트립 통합 테스트 (대표 PRD)

**Files:**
- Create: `test/integration.test.ts`

**Interfaces:**
- Consumes: parse/render/read/serialize 전부
- Produces: 성공 기준 §11-1,2를 자동 검증하는 통합 테스트.

- [ ] **Step 1: 실패/통과 테스트 작성**

`test/integration.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { parseMarkdown } from '../src/core/parse';
import { serializeDoc } from '../src/core/serialize';
import { renderDoc } from '../src/core/render';
import { readDoc } from '../src/core/read';
import { FakeFigma } from './fake-figma';

const PRD = [
  '# 샘플 문서',
  '',
  '> 이 문서는 라운드트립 예시임',
  '> 두 번째 인용 줄',
  '',
  '## 1. 개요',
  '',
  '이 문단에는 **굵은** 글자와 [링크](http://example.com)가 있다.',
  '',
  '- 첫 번째 항목',
  '- 두 번째 항목',
  '  - 중첩 항목',
  '',
  '| 열 A | 열 B | 열 C |',
  '| --- | --- | --- |',
  '| 1 | 2 | 3 |',
  '',
  '```ts',
  'const enabled = true;',
  '```',
].join('\n');

const norm = (s: string) => s.replace(/\r\n?/g, '\n').trim();

describe('full pipeline roundtrip (PRD)', () => {
  it('md → render → read → md 의미 보존', async () => {
    const doc = parseMarkdown(PRD);
    const page = await renderDoc(doc, new FakeFigma());
    const { doc: back, warnings } = readDoc(page);
    expect(warnings).toEqual([]);
    expect(norm(serializeDoc(back))).toBe(norm(PRD));
  });

  it('꼬리표 없는 이물질은 무시 + 경고', async () => {
    const doc = parseMarkdown('# A');
    const figma = new FakeFigma();
    const page = await renderDoc(doc, figma);
    const foreign = figma.createFrame(); foreign.name = '직접 그린 사각형';
    page.appendChild(foreign);
    const { warnings } = readDoc(page);
    expect(warnings.length).toBe(1);
    expect(warnings[0]).toContain('직접 그린 사각형');
  });
});
```

- [ ] **Step 2: Run**

Run: `npx vitest run test/integration.test.ts`
Expected: PASS 2건. 실패 시 어느 블록에서 왕복이 깨지는지 좁혀서(케이스를 줄여) 원인 블록의 parse/serialize/render/read를 점검.

- [ ] **Step 3: 전체 테스트 + 빌드 확인**

Run: `npm test && npm run build`
Expected: 전체 PASS, `dist/code.js` 생성.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "test: 전체 파이프라인 라운드트립 통합 테스트 (PRD)"
```

---

### Task 15: UI 패널 + 코드↔UI 메시지 배선

**Files:**
- Modify: `src/code.ts`, `src/ui.html`
- Create: `src/ui.ts`, `esbuild.mjs`(UI 번들 추가)

**Interfaces:**
- Consumes: `renderDoc`, `readDoc`, `serializeDoc`, `parseMarkdown`
- Produces: 동작하는 플러그인 — `마크다운 → 렌더`, `익스포트 → 마크다운` 버튼.

메시지 프로토콜(확정):
- UI→code: `{ type: 'render', md: string }` / `{ type: 'export' }`
- code→UI: `{ type: 'exported', md: string, warnings: string[] }` / `{ type: 'error', message: string }`

- [ ] **Step 1: esbuild가 UI도 번들하도록 수정**

`esbuild.mjs` 교체:
```js
import { build } from 'esbuild';
import { readFileSync, writeFileSync } from 'node:fs';

await build({ entryPoints: { code: 'src/code.ts' }, bundle: true, target: 'es2017', format: 'iife', outfile: 'dist/code.js', logLevel: 'info' });
const js = (await build({ entryPoints: ['src/ui.ts'], bundle: true, target: 'es2017', format: 'iife', write: false })).outputFiles[0].text;
const html = readFileSync('src/ui.html', 'utf8').replace('</body>', `<script>${js}</script></body>`);
writeFileSync('dist/ui.html', html);
console.log('built dist/code.js + dist/ui.html');
```

- [ ] **Step 2: code.ts 구현 (메인 스레드)**

`src/code.ts`:
```ts
import { parseMarkdown } from './core/parse';
import { serializeDoc } from './core/serialize';
import { renderDoc } from './core/render';
import { readDoc } from './core/read';
import type { FrameLike } from './core/figma-like';

figma.showUI(__html__, { width: 380, height: 560 });

// figma 전역을 FigmaLike로 어댑트: 실제 API가 서브셋을 만족한다.
const adapter = {
  createFrame: () => figma.createFrame() as unknown as FrameLike,
  createText: () => figma.createText() as any,
  loadFontAsync: (f: { family: string; style: string }) => figma.loadFontAsync(f),
};

figma.ui.onmessage = async (msg: { type: string; md?: string }) => {
  try {
    if (msg.type === 'render') {
      const doc = parseMarkdown(msg.md ?? '');
      const page = await renderDoc(doc, adapter as any);
      const node = page as unknown as FrameNode;
      figma.currentPage.appendChild(node);
      figma.viewport.scrollAndZoomIntoView([node]);
      figma.currentPage.selection = [node];
    } else if (msg.type === 'export') {
      const sel = figma.currentPage.selection[0];
      const page = (sel && sel.type === 'FRAME' ? sel : findLatestPage()) as unknown as FrameLike | null;
      if (!page) { figma.ui.postMessage({ type: 'error', message: '내보낼 Fig.md 페이지 프레임을 선택하세요.' }); return; }
      const { doc, warnings } = readDoc(page);
      figma.ui.postMessage({ type: 'exported', md: serializeDoc(doc), warnings });
    }
  } catch (e) {
    figma.ui.postMessage({ type: 'error', message: String(e) });
  }
};

function findLatestPage(): SceneNode | null {
  const frames = figma.currentPage.children.filter(n => n.type === 'FRAME' && n.getPluginData('figmd:block') === 'page');
  return (frames[frames.length - 1] as SceneNode) ?? null;
}
```

- [ ] **Step 3: ui.html / ui.ts 구현**

`src/ui.html`:
```html
<!doctype html><html><head><meta charset="utf-8"><style>
  body { font: 13px -apple-system, sans-serif; margin: 0; padding: 12px; }
  textarea { width: 100%; height: 300px; box-sizing: border-box; font-family: monospace; }
  button { margin: 6px 4px 0 0; padding: 8px 12px; cursor: pointer; }
  #warn { color: #b30; white-space: pre-wrap; margin-top: 8px; }
</style></head><body>
  <textarea id="md" placeholder="여기에 마크다운을 붙여넣으세요"></textarea>
  <div>
    <button id="render">마크다운 → 렌더</button>
    <button id="export">익스포트 → 마크다운</button>
  </div>
  <div id="warn"></div>
</body></html>
```

`src/ui.ts`:
```ts
const $ = (id: string) => document.getElementById(id) as HTMLElement;
const md = () => ($('md') as HTMLTextAreaElement);
const warn = () => $('warn');

$('render').onclick = () => parent.postMessage({ pluginMessage: { type: 'render', md: md().value } }, '*');
$('export').onclick = () => parent.postMessage({ pluginMessage: { type: 'export' } }, '*');

onmessage = (e: MessageEvent) => {
  const m = e.data.pluginMessage;
  if (!m) return;
  if (m.type === 'exported') {
    md().value = m.md;
    md().select();
    document.execCommand('copy');
    warn().textContent = m.warnings.length ? '경고(건너뜀):\n' + m.warnings.join('\n') : '내보냄 + 클립보드 복사 완료';
  } else if (m.type === 'error') {
    warn().textContent = '오류: ' + m.message;
  }
};
```

- [ ] **Step 4: 빌드 검증**

Run: `npm run build`
Expected: `dist/code.js`, `dist/ui.html` 생성, 에러 없음.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: UI 패널 + code↔UI 메시지 배선 (렌더/익스포트 버튼)"
```

---

### Task 16: 블록 삽입 "+" 메뉴 (구분선·표)

**Files:**
- Modify: `src/code.ts`, `src/ui.html`, `src/ui.ts`

**Interfaces:**
- Consumes: `renderDoc`(단일 블록 렌더 재사용은 하지 않음; 대신 code.ts에서 최소 삽입 헬퍼)
- Produces: 선택 프레임에 구분선/빈 표를 꼬리표 달린 블록으로 삽입.

- [ ] **Step 1: 메시지 타입 확장 + UI 버튼 추가**

`src/ui.html`의 버튼 묶음에 추가:
```html
    <button id="ins-divider">+ 구분선</button>
    <button id="ins-table">+ 표(2x2)</button>
```
`src/ui.ts`에 추가:
```ts
$('ins-divider').onclick = () => parent.postMessage({ pluginMessage: { type: 'insert', block: 'divider' } }, '*');
$('ins-table').onclick = () => parent.postMessage({ pluginMessage: { type: 'insert', block: 'table' } }, '*');
```

- [ ] **Step 2: code.ts 삽입 처리**

`figma.ui.onmessage` 안 `export` 분기 뒤에 추가:
```ts
    else if (msg.type === 'insert') {
      const sel = figma.currentPage.selection[0];
      const page = (sel && sel.type === 'FRAME' && sel.getPluginData('figmd:block') === 'page') ? sel : findLatestPage();
      if (!page || page.type !== 'FRAME') { figma.ui.postMessage({ type: 'error', message: 'Fig.md 페이지를 선택하세요.' }); return; }
      const block = (msg as any).block === 'table'
        ? { type: 'table', header: ['열1', '열2'], rows: [['', '']] } as const
        : { type: 'divider' } as const;
      const doc = { blocks: [block] };
      const tmp = await renderDoc(doc as any, adapter as any);
      const child = (tmp as any).children[0] as SceneNode;
      (page as FrameNode).appendChild(child);
    }
```
(주: `renderDoc`은 페이지 프레임을 만들고 그 첫 자식이 우리가 원하는 블록이다. 임시 페이지의 자식을 실제 페이지로 옮긴다.)

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 에러 없음.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: 블록 삽입 + 메뉴 (구분선·2x2 표)"
```

---

### Task 17: 실제 피그마 수동 QA + README

**Files:**
- Create: `README.md`, `docs/qa-checklist.md`

**Interfaces:**
- Consumes: 전체 빌드물
- Produces: 실기기 검증 기록 + 사용법 문서.

- [ ] **Step 1: README 작성**

`README.md`:
```markdown
# Fig.md

피그마 프레임을 노션 페이지처럼 쓰는 마크다운 라운드트립 에디터.

## 개발
- `npm install`
- `npm test` — 코어/렌더/읽기/통합 테스트
- `npm run build` — `dist/code.js`, `dist/ui.html` 생성

## 피그마에 로드
1. Figma 데스크톱 → Plugins → Development → Import plugin from manifest
2. 이 폴더의 `manifest.json` 선택
3. 플러그인 실행 → 마크다운 붙여넣기 → "마크다운 → 렌더"
4. 캔버스에서 편집 → 페이지 프레임 선택 → "익스포트 → 마크다운"(클립보드 복사)

## 범위
지원: 제목, 문단, 굵게/기울임/코드/링크, 목록(중첩·체크박스), 인용, 구분선, 코드블록, 표, 이미지(플레이스홀더).
미지원(v1): 토글 접기, 표 행/열 편집, 실시간 동기화.
```

- [ ] **Step 2: QA 체크리스트 작성 및 실기기 수행**

`docs/qa-checklist.md`에 아래 항목을 적고, Figma 데스크톱에서 직접 수행하며 결과를 체크한다.
```markdown
# Fig.md 수동 QA (실제 Figma)

- [ ] manifest import 성공, 패널 표시
- [ ] 대표 PRD 붙여넣기 → 렌더 → 프레임이 세로 오토레이아웃으로 생성
- [ ] 제목/본문/굵게/링크가 시각적으로 구분됨
- [ ] 표가 그리드로 렌더됨
- [ ] 캔버스에서 제목 텍스트 수정 → 익스포트 시 반영됨
- [ ] "+ 구분선" 삽입 → 블록 사이에 들어가고 드래그로 이동 가능
- [ ] 블록 순서를 드래그로 바꾼 뒤 익스포트 → 순서 반영됨
- [ ] 페이지 안에 사각형 직접 그리고 익스포트 → 경고에 뜨고 md에는 안 들어감
- [ ] 익스포트 결과를 노션/협업툴에 붙여넣기 → 깨지지 않음
```

- [ ] **Step 3: 발견 이슈가 있으면 최소 수정 후 재빌드**

실기기에서 깨지는 항목이 있으면, 해당 블록의 render/read를 좁혀 수정하고 `npm test`로 회귀 확인 후 재빌드. 폰트 미로드 오류 시 `render.ts`의 로드 폰트 목록에 실제 사용 폰트를 추가.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "docs: README + 수동 QA 체크리스트, 실기기 검증"
```

---

## Self-Review (작성자 점검 결과)

**1. 스펙 커버리지**
- 꼬리표 미러 아키텍처 → Task 10~13. 오토레이아웃 페이지 → Task 11(renderDoc). 명시적 임포트/익스포트 → Task 15. 블록 삽입 GUI → Task 16. 이물질 무시+경고 → Task 11(readBlock) + Task 14(테스트). 표 왕복 → Task 9(md) + Task 13(노드). v1 문법 전체 → Task 3~9(코어) + Task 11~13(노드). 성공 기준 §11 → Task 14 통합 테스트. 저장 위치/독립 git → Global Constraints.
- 갭: 이미지 편집(스펙 §6에서 "—", 비목표) — 플레이스홀더 렌더만, 의도된 제외.
- 갭: 전체 재렌더 정책(스펙 §4.1) — Task 15 `render`가 새 프레임을 append(기존 것 보존/덮어쓰기 UI는 v1 단순화). README에 명시.

**2. 플레이스홀더 스캔**
- 코드 스텁 2곳(Task 11의 표 case, read 표 case)은 Task 13에서 실제 구현으로 교체되며, 그 사이에도 컴파일·테스트가 통과하도록 유효한 값을 반환한다. "TODO"식 미완성 아님.
- Task 2의 `if (r.em == null) {}`와 Task 11의 잘못된 타입 표기는 본문에서 "삭제/교체" 지시로 명시함.

**3. 타입 일관성**
- `renderDoc`/`readDoc`/`parseMarkdown`/`serializeDoc`/`flattenInlines`/`runsToInlines`/`parseInlines`/`serializeInlines`/`setBlockTag`/`getBlockTag`/`setCellTag`/`getCellTag` — 모듈 계약과 전 Task에서 이름 일치 확인.
- `Run` 필드(text/bold/italic/code/href) 전 Task 일치. `Doc/Block/List/ListItem/Table/Inline` 일치.

## 알려진 리스크 / 실기기에서만 검증 가능한 것
- 실제 `figma` 전역이 `FigmaLike` 서브셋을 만족하는지는 Task 15 어댑터 + Task 17 수동 QA에서 최종 확인(타입 캐스팅 `as any` 최소 사용).
- 폰트(`Inter`, `Roboto Mono`) 가용성은 환경 의존 → 미로드 시 Task 17에서 로드 목록 조정.
- 표의 시각적 정렬(열 너비 등)은 v1에서 최소(오토레이아웃 기본). 미관 개선은 v2.
